[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pointretta"
version = "0.1.0"
description = "PointRetta: retention-based LLM, O(N) training O(1) inference"
requires-python = ">=3.8"
